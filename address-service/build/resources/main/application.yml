server:
  port: 8881
  servlet:
    context-path: /address

spring:
  profiles:
    active: 'k8s'
  application:
    name: address-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
